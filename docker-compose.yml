services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: easydocs
      POSTGRES_PASSWORD: easydocs123
      POSTGRES_DB: easydocs_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: